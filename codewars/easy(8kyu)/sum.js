var summation = function (num) {
    // Code here
    if(typeof num !== 'number') return "impossible "
    var sum=0
    for (let i = 0 ; i<num+1 ; i++){
       sum+=i;
    }
    return sum
  }

  var summation = function (num) {
    // Code here
    if(typeof num !== 'number') return "impossible "
   

    return n*(n+1) /2
  }